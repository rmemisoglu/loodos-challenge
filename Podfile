# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'loodos-challenge' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '~> 5.2'
    pod ‘Firebase’
    pod ‘Firebase/Analytics’
    pod ‘Firebase/RemoteConfig’
  # Pods for loodos-challenge

  target 'loodos-challengeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'loodos-challengeUITests' do
    # Pods for testing
  end

end
